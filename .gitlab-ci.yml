stages:
  - train
  - build
  - gitops
  - retrain

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""
  IMAGE_NAME: $CI_REGISTRY_IMAGE
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA

train-model:
  stage: train
  image: python:3.11-slim
  before_script:
    - python --version
    - pip install --no-cache-dir -r requirements.txt
  script:
    - python model/train.py
    - ls -l model/
  artifacts:
    paths:
      - model/model.pkl
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_BRANCH

build-image:
  stage: build
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -t "$IMAGE_NAME:$IMAGE_TAG" .
    - docker tag "$IMAGE_NAME:$IMAGE_TAG" "$IMAGE_NAME:latest"
    - docker push "$IMAGE_NAME:$IMAGE_TAG"
    - docker push "$IMAGE_NAME:latest"
  needs:
    - job: train-model
      artifacts: true
  rules:
    - if: $CI_COMMIT_BRANCH

update-helm-values:
  stage: gitops
  image: alpine:3.20
  before_script:
    - apk add --no-cache bash git sed
    - git config --global user.email "gitlab-ci@example.com"
    - git config --global user.name "GitLab CI"
  script:
    - chmod +x scripts/update_image_tag.sh
    - ./scripts/update_image_tag.sh "$CI_COMMIT_SHORT_SHA"
    - grep -A3 '^image:' helm/values.yaml
    - git checkout "$CI_COMMIT_BRANCH"
    - git add helm/values.yaml
    - |
      if git diff --cached --quiet; then
        echo "No changes to commit"
      else
        git commit -m "Update Helm image tag to $CI_COMMIT_SHORT_SHA [skip ci]"
        git remote set-url origin "https://oauth2:${GITHUB_TOKEN}@github.com/vartsab/MLOpsCICD-final-project.git"
        git push origin HEAD:$CI_COMMIT_BRANCH
      fi
  needs:
    - job: build-image
  rules:
    - if: $CI_COMMIT_BRANCH == "final-project"

retrain-model:
  stage: retrain
  image: python:3.11-slim
  before_script:
    - python --version
    - pip install --no-cache-dir -r requirements.txt
  script:
    - echo "Manual retrain started"
    - python model/train.py
    - ls -l model/
    - echo "Retrain finished. Next step is image rebuild/push via pipeline."
  artifacts:
    paths:
      - model/model.pkl
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_BRANCH == "final-project"
      when: manual
